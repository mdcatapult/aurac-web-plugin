import { RecogniserNamePipe } from './recogniser-name.pipe'

describe('RecogniserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RecogniserNamePipe()
    expect(pipe).toBeTruthy()
  })
})
