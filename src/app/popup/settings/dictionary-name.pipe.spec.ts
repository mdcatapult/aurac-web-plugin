import { DictionaryNamePipe } from './dictionary-name.pipe';

describe('DictionaryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DictionaryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
